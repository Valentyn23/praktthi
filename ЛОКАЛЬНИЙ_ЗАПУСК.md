# 🚀 Інструкція з локального запуску SecureVision

## 📋 Вимоги

- **Python 3.8+** (перевірте: `python --version` або `python3 --version`)
- **pip** (менеджер пакетів Python)
- **Git** (опціонально, для клонування репозиторію)

---

## 🎯 Швидкий старт

### Для Windows:

1. Відкрийте термінал (Command Prompt або PowerShell) в папці проекту
2. Запустіть:
   ```cmd
   start.bat
   ```

### Для Linux/Mac:

1. Відкрийте термінал в папці проекту
2. Запустіть:
   ```bash
   ./start.sh
   ```
   або
   ```bash
   bash start.sh
   ```

### Універсальний спосіб (Python скрипт):

```bash
python run_local.py
```
або
```bash
python3 run_local.py
```

---

## 📦 Що роблять скрипти?

Скрипти автоматично:

1. ✅ Перевіряють наявність Python
2. 📦 Створюють віртуальне середовище (venv) якщо потрібно
3. 📥 Встановлюють всі залежності з `requirements.txt`
4. 📁 Створюють папку `logs/` для логів
5. 🗄️ Ініціалізують базу даних SQLite
6. 🚀 Запускають Telegram бота та веб-додаток одночасно

---

## 🔗 Доступ до додатків

Після успішного запуску:

- 🌐 **Веб-додаток**: http://localhost:5000
- 📱 **Telegram бот**: Відкрийте Telegram і знайдіть свого бота
- 📋 **Логи**: `logs/bot.log` та `logs/webapp.log`

---

## 🛑 Зупинка додатків

- Натисніть `Ctrl+C` в терміналі де запущено скрипт
- Або просто закрийте вікна терміналів

---

## ⚙️ Ручний запуск (для розробників)

### 1. Створення віртуального середовища:

```bash
python -m venv venv
```

### 2. Активація віртуального середовища:

**Windows:**
```cmd
venv\Scripts\activate
```

**Linux/Mac:**
```bash
source venv/bin/activate
```

### 3. Встановлення залежностей:

```bash
pip install -r requirements.txt
```

### 4. Запуск бота (в окремому терміналі):

```bash
python bot.py
```

### 5. Запуск веб-додатку (в окремому терміналі):

```bash
python webapp.py
```

---

## 🔧 Налаштування

### Зміна токена бота:

Відредагуйте файл `config.py`:

```python
BOT_TOKEN = "ваш_токен_бота"
```

### Зміна порту веб-додатку:

Відредагуйте файл `webapp.py` (остання стрічка):

```python
app.run(debug=True, port=5000, host='0.0.0.0')  # Змініть 5000 на інший порт
```

---

## 📊 Структура логів

Логи зберігаються в папці `logs/`:

- `bot.log` - логи Telegram бота
- `webapp.log` - логи веб-додатку

Кожен файл логів:
- Максимальний розмір: 5 MB
- Кількість backup файлів: 5
- Автоматична ротація при досягненні ліміту

---

## 🐛 Вирішення проблем

### Python не знайдено:

**Windows:**
1. Встановіть Python з [python.org](https://www.python.org/downloads/)
2. Під час встановлення відмітьте "Add Python to PATH"

**Linux:**
```bash
sudo apt-get update
sudo apt-get install python3 python3-pip python3-venv
```

**Mac:**
```bash
brew install python3
```

### Помилки при встановленні залежностей:

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### Порт 5000 вже зайнятий:

1. Змініть порт у файлі `webapp.py`
2. Або закрийте додаток що використовує порт 5000

### База даних заблокована:

```bash
# Видаліть файл БД та перезапустіть
rm db.sqlite3
```

---

## 📞 Підтримка

- 📧 Email: info@securevision.ua
- 📱 Telegram: @SecureVisionBot
- 📞 Телефон: +380 44 123 45 67

---

## 📝 Примітки

- Це демонстраційна версія з імітованою системою оплати
- Дані зберігаються в локальній БД SQLite (`db.sqlite3`)
- Для production використання потрібна додаткова настройка безпеки

---

**SecureVision** © 2024 | Всі права захищено
